# Heavy ML/OCR packages — PyTorch, models, image processing
# These are installed in a SEPARATE Docker layer so they are cached
# and never re-downloaded unless THIS file changes.
docling>=2.0.0
rapidocr>=3.0.0
pypdfium2>=4.30.0
numpy>=1.26.0
Pillow>=10.0.0
